// 29-8-18
//https://www.codechef.com/problems/FLIPCOIN

#include<stdio.h>
#include<iostream>
#include<stack>
#include<utility>
using namespace std;

int main(){
	int n, q;
	scanf("%d%d", &n,&q);
	int a[n][2];
	for (size_t i = 0; i < n; i++) {
		a[i][0] = -1;
		a[i][1] = -1;
	}
	std::stack<std::pair<int, int>> s;
	while (q--) {
		int x, y, z;
		scanf("%d%d%d", &x, &y, &z);
		
	}
}
