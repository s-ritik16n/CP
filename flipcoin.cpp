#include<stdio.h>

int main(){
	int n, q;
	scanf("%d%d", &n,&q);
	int i,a[n];
	for(i = 0;i < n; i++) a[i] = -1; 
	while(q--){
		int a, b, c;
		scanf("%d%d%d", &a, &b, &c);
		if(a == 0){

		}
		else{
		
		}
	}
}
